use bevy::prelude::*;

use crate::features::cards::SideSchemeCard;

pub fn spawn_side_scheme_detail(
    children_builder: &mut ChildBuilder,
    asset_server: Res<AssetServer>,
    card: SideSchemeCard<'static>,
) {
}
