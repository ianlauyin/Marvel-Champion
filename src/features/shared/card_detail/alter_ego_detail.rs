use bevy::prelude::*;

use crate::features::cards::AlterEgoCard;

pub fn spawn_alter_ego_detail(
    children_builder: &mut ChildBuilder,
    asset_server: Res<AssetServer>,
    card: AlterEgoCard<'static>,
) {
}
