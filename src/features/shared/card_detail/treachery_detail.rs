use bevy::prelude::*;

use crate::features::cards::TreacheryCard;

pub fn spawn_treachery_detail(
    children_builder: &mut ChildBuilder,
    asset_server: Res<AssetServer>,
    card: TreacheryCard<'static>,
) {
}
