use bevy::prelude::*;

use crate::features::cards::MainSchemeCard;

pub fn spawn_main_scheme_detail(
    children_builder: &mut ChildBuilder,
    asset_server: Res<AssetServer>,
    card: MainSchemeCard<'static>,
) {
}
