use bevy::prelude::*;

use crate::features::cards::VillainCard;

pub fn spawn_villain_detail(
    children_builder: &mut ChildBuilder,
    asset_server: Res<AssetServer>,
    card: VillainCard<'static>,
) {
}
